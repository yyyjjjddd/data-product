<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.metrics.mapper.TaskMapper">

    <resultMap id="TaskResultMap" type="com.example.metrics.model.entity.Task">
        <id property="id" column="id"/>
        <result property="taskId" column="task_id"/>
        <result property="metricId" column="metric_id"/>
        <result property="status" column="status"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="resultData" column="result_data"/>
        <result property="errorMessage" column="error_message"/>
        <result property="retryCount" column="retry_count"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.metrics.model.entity.Task"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO task (task_id, metric_id, status, retry_count,
                          created_at, updated_at)
        VALUES (#{taskId}, #{metricId}, #{status}, #{retryCount},
                NOW(), NOW())
    </insert>

    <update id="updateById" parameterType="com.example.metrics.model.entity.Task">
        UPDATE task
        SET status = #{status},
            start_time = #{startTime},
            end_time = #{endTime},
            result_data = #{resultData},
            error_message = #{errorMessage},
            retry_count = #{retryCount},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="selectByTaskId" resultMap="TaskResultMap">
        SELECT * FROM task WHERE task_id = #{taskId}
    </select>

    <select id="selectByMetricId" resultMap="TaskResultMap">
        SELECT * FROM task WHERE metric_id = #{metricId} ORDER BY created_at DESC
    </select>

    <select id="selectByStatus" resultMap="TaskResultMap">
        SELECT * FROM task WHERE status = #{status} ORDER BY created_at DESC
    </select>

    <select id="selectByPage" resultMap="TaskResultMap">
        SELECT * FROM task
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="metricId != null">
                AND metric_id = #{metricId}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByCondition" resultType="long">
        SELECT COUNT(*) FROM task
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="metricId != null">
                AND metric_id = #{metricId}
            </if>
        </where>
    </select>

    <update id="updateStatus">
        UPDATE task
        SET status = #{status}, updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="updateResult">
        UPDATE task
        SET status = #{status},
            result_data = #{resultData},
            error_message = #{errorMessage},
            updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <select id="selectPendingOrRunningByMetricId" resultMap="TaskResultMap">
        SELECT * FROM task
        WHERE metric_id = #{metricId}
          AND (status = 'pending' OR status = 'running')
        ORDER BY created_at DESC
        LIMIT 1
    </select>

</mapper>
